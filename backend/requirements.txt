# Core backend
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# Vector DB & Embeddings
qdrant-client>=1.8.0
FlagEmbedding>=1.2.0
sentence-transformers>=2.5.0
torch>=2.2.0

# Kannada text normalization (indic_preprocess.py). Has a regex-based
# fallback if this is missing, but it should be installed so the real
# normalizer is used, not the fallback.
indic-nlp-library>=0.92

# LLM (current default: Groq hosted)
groq>=0.9.0

# LLM (OpenRouter backend — cross-judge evaluation, model bake-off)
openai>=1.30.0

# LLM (optional local backend — currently untested on Windows+Python 3.13)
# ollama>=0.1.7

# Database
motor>=3.3.0
pymongo>=4.5.0  # sync client, used directly by preflight.py (motor is async-only)

# Evaluation
sacrebleu>=2.4.0
numpy>=1.26.0
rank_bm25>=0.2.2
rouge-score>=0.1.2


# Dev
pytest>=8.0.0
